# bootstrap.ps1 — IGA Marketing Master 2.0 first-time setup (Windows / PowerShell).
#
# Idempotent setup script that a non-technical operator can run to install the
# tool fresh. Will create a Python 3.13 venv, install pinned dependencies from
# requirements.txt, run `playwright install chromium`, and verify the install
# with a health check.
#
# TODO: implementation pending — developer agents will fill this in during
# Phase 1 (Foundation).
