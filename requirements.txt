# IGA Marketing Master 2.0 — Python dependencies.
# Pinned versions to be finalized by developer agents during Phase 1; ranges
# below reflect the floor established by PLAN-REVIEW.md research findings.

anthropic
playwright>=1.55
PySide6>=6.6
pypdf
keyring
platformdirs
# pywin32 — removed: no module under src/ imports it directly.
# `keyring` handles DPAPI access via its own `pywin32-ctypes` transitive
# dependency, so a top-level pywin32 pin is redundant.
# psutil — used by epic_session.cleanup_user_data_dir_lock for PID-liveness detection
# (graceful fallback if absent, but recommended on Windows)
psutil>=5.9
